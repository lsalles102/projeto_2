// Supabase is disabled for development mode
// The client-side still has Supabase configured for UI components
// but server-side uses in-memory storage only

export const supabaseAdmin = null;
export const supabase = null;