Tenho um site (Node.js + Express + Supabase Client) que vende licenças de software.

Após o usuário realizar um pagamento (via Mercado Pago), meu backend recebe o Webhook e gera uma chave de licença (exemplo: FOVD-TEST-1749957330299-53EDEBC6).

Essa chave é salva no banco de dados, em uma tabela chamada licenses, e depois enviada por e-mail ao comprador.

❌ Problema atual:
Quando o usuário tenta ativar a licença, ele recebe o erro:

❌ Esta chave já está vinculada a outro usuário (exemplo: user_id = 8)

Mas o comprador correto é outro (exemplo: user_id = 6).

✅ O que precisa ser corrigido:
Atualmente, durante o processamento do Webhook, ao criar a licença, o código está vinculando a chave ao usuário errado (user_id errado).

Acredito que o erro está na forma como estou buscando o usuário ou como estou definindo o user_id antes de salvar no banco.

✅ Como deveria funcionar:
Quando eu crio o link de pagamento no Mercado Pago, eu passo o external_reference contendo o user.id do comprador (ou algum identificador único para localizar o usuário depois).

Quando o Webhook de pagamento chega, ele traz o campo external_reference no payload.

Meu backend deve buscar na tabela users o usuário cujo external_reference bate com o recebido.

Só depois disso, gerar a licença e salvar na tabela licenses, vinculando ao campo user_id correto.

✅ Estrutura aproximada das tabelas no Supabase:
Tabela users:
id	email	external_reference
6	luizsalles102@gmail.com	payment_g1EwidZ-nnpy19dZStUDZ

Tabela licenses:
id	key	user_id	plan	expires_at
1	FOVD-TEST-XXXXXX	6	test	2025-06-15

✅ Trecho atual com problema (exemplo simplificado):
typescript
Copiar
Editar
await supabase.from('licenses').insert({
  key: generatedKey,
  user_id: 8,  // ← Aqui está o problema! Está usando o usuário errado!
  plan: 'test',
  expires_at: expirationDate,
});
✅ O que quero como resultado:
👉 Código corrigido do Webhook /api/payments/webhook com:

Busca correta do user_id com base no external_reference:

Exemplo:

typescript
Copiar
Editar
const { data: user, error } = await supabase
  .from('users')
  .select('*')
  .eq('external_reference', payment.external_reference)
  .single();
Só criar a licença se o user for encontrado.

Log detalhado com:

ID do pagamento

E-mail do usuário

ID do usuário

Chave gerada

external_reference usado

Caso o usuário não seja encontrado → retornar erro e logar o problema.

✅ Exemplo de Fluxo Correto:
Webhook recebe pagamento com external_reference = payment_g1EwidZ-nnpy19dZStUDZ

Backend busca o usuário no Supabase:

typescript
Copiar
Editar
SELECT * FROM users WHERE external_reference = 'payment_g1EwidZ-nnpy19dZStUDZ';
Gera a chave de licença.

Insere na tabela licenses com o user.id correto.

Envia o e-mail.